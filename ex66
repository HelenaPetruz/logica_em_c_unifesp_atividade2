#include <stdio.h>

int main()
{
    int a, b, soma = 0;
    
    scanf("%d %d", &a, &b);
    if(a > b){
        int temp = a;
        a = b;
        b = temp;
    }
    for(int i = a; i <= b; i++){
        soma += i;
    }
}
